# When running docker compose for the first time

Set BUILD_GRAPHS=True in the docker-compose.yml file

After running for the first time
Set BUILD_GRAPHS=False

Best.

Initial startup will take ~10minutes